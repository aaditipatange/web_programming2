import React from 'react';
import '../App.css';

const Error = (props) => {
    //console.log(props.type);
	return (
		<div>
			<h2>
				404 Page not found error.
			</h2>
		</div>
	);
};

export default Error;